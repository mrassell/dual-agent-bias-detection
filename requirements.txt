# Demo server requirements (Railway deployment).
# No torch/transformers — the demo uses cloud LLM APIs only.
# For local eval scripts (RoBERTa, NLI), use: pip install -r requirements-full.txt
flask>=3.0
flask-cors>=4.0
openai>=1.0
anthropic>=0.40
mcp[cli]>=1.0
pydantic>=2
numpy
pandas>=2.0
scikit-learn>=1.3
